# 固定访问地址（请收藏）

## 桌面没有图标？

必须在**你自己的 Windows 教室电脑**上操作（不是 Cloud Agent 网页里）。

1. 打开本机 Photon 项目文件夹（更新到最新）
2. 双击 `一键放到桌面.bat` 或 `PUT-ON-DESKTOP.bat`
3. 桌面应出现 `GY-Start.bat` / `光影盟-开始上课.bat`
4. 脚本会自动打开桌面文件夹给你看
5. 还是没有：手动把项目里的 `start.bat` 复制到桌面

---

## 老师端（永远不变）

```
http://127.0.0.1:3000/?mode=teacher
```

浏览器收藏这一条。上课：先桌面「开始上课」，再打开它。

## 学生端（本机）

```
http://127.0.0.1:3000/?mode=student
```

## 手机扫码

首次绑定后写入 `gy-classroom.json` 并永久锁定。  
当前值见项目目录 `固定访问地址.txt`（每次启动生成）。
