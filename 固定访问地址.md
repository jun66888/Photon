# 固定访问地址（请收藏）

## 老师端（永远不变）

```
http://127.0.0.1:3000/?mode=teacher
```

浏览器收藏这一条即可。每次双击 `start.bat` 后打开它，或双击 `打开老师端.bat` / `打开老师端.url`。

## 学生端（本机）

```
http://127.0.0.1:3000/?mode=student
```

## 手机扫码

手机扫码地址在首次连上教室 Wi‑Fi 并启动后，会写入本机 `gy-classroom.json` 并**永久锁定**，不会每天换。

查看当前锁定值：打开同目录 `固定访问地址.txt`（每次启动自动生成）。

若手机扫不开且确认电脑 IP 已变：给电脑设回原来的静态 IP，或运行一次 `绑定课堂扫码地址.bat` 手动重绑（仅此才会更换扫码地址）。

## 不要用

临时公网链接、Cloud Agent 预览、每天不一样的隧道域名。
