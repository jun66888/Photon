# 固定访问地址（请收藏）

## Mac：文件位置

项目根目录（与 `index.html` 同级）：

- 开始上课：`start.sh`
- 放到桌面：`一键放到桌面.command` 或 `一键放到桌面.sh`
- 打开老师端：`打开老师端.command`

双击 `一键放到桌面.command` 后，桌面会有 `GY-Start.command` / `光影盟-开始上课.command`。

若双击无反应：打开「终端」进入项目目录，执行：

```bash
chmod +x start.sh 一键放到桌面.sh 一键放到桌面.command
./一键放到桌面.sh
```

---

## 老师端（永远不变）

```
http://127.0.0.1:3000/?mode=teacher
```

## 学生端（本机）

```
http://127.0.0.1:3000/?mode=student
```
