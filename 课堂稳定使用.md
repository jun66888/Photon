# 本机离线课堂（地址永久固定）

## 桌面没有图标时

我这边（云端）**不能**直接改你教室电脑的桌面。请在**你自己的 Windows 电脑**上操作：

1. 打开本机上的 Photon 项目文件夹（先 `git pull` 或重新拷贝最新版）
2. 确认文件夹里有：`一键放到桌面.bat` 或 `PUT-ON-DESKTOP.bat`
3. **双击**其中一个
4. 会自动打开桌面文件夹；应看到：
   - `光影盟-开始上课.bat` 或 `GY-Start.bat` ← 上课点这个
   - `光影盟-打开老师端.bat` 或 `GY-Teacher.bat`
   - `光影盟-收藏地址.txt`
5. 若仍没有：把项目里的 `start.bat` **手动复制到桌面**即可

`start.bat` 本身在项目根目录（和 `index.html` 同一层）。

---

## 请收藏这一条（永远不变）

```
http://127.0.0.1:3000/?mode=teacher
```

---

## 每节课

1. 电脑连教室 Wi‑Fi（可无外网）
2. 桌面双击「光影盟-开始上课」或 `GY-Start`（窗口一直开着）
3. 打开收藏的老师端地址，或双击「光影盟-打开老师端」
4. 手机扫码用已锁定地址（见项目里 `固定访问地址.txt`）

首次连教室网启动时会自动绑定扫码地址。  
只有你主动运行「绑定扫码」才会更换扫码地址。  
建议给电脑设静态 IP。

---

## 一次准备

1. 安装 Node.js LTS（有网时一次）
2. 双击 `一键放到桌面.bat`
3. 双击一次 `开放防火墙.bat`（或桌面上的同名入口）
4. 浏览器收藏 `http://127.0.0.1:3000/?mode=teacher`

---

## 不要用

临时公网 / Cloud Agent 预览 / 每天不一样的隧道链接。
