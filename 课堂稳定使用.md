# 课堂稳定使用（本机方案）

临时公网隧道（`*.trycloudflare.com`）会换域名、会掉线，**不能**当正式课堂入口。  
正式上课只在**老师自己的电脑**上跑，手机连同一 Wi‑Fi，用局域网固定地址扫码。

---

## 一次准备（教室电脑）

1. 安装 [Node.js LTS](https://nodejs.org/)（只需一次）
2. 把本仓库放到电脑本地（U 盘拷贝或 `git clone` 均可）
3. 双击一次 **`开放防火墙.bat`**（允许手机访问 3000 端口）
4. （强烈建议）给电脑网卡设置**静态 IP**，或在路由器里按 MAC 做 DHCP 保留  
   - 这样 `http://192.168.x.x:3000` 长期不变，打印的二维码可以反复用

---

## 每节课怎么用

1. 电脑连教室 Wi‑Fi（与学生手机同一网络；不要用访客隔离网）
2. 双击 **`start.bat`**（窗口保持打开）
3. 浏览器自动打开老师端：`http://localhost:3000/?mode=teacher`
4. 看黑色窗口里的 **「★ 固定扫码」** 地址（形如 `http://192.168.1.8:3000`）
5. 签到页点「开启签到」——二维码会使用该固定地址  
6. 下课：关窗口或 Ctrl+C

服务异常退出时，`start.bat` 会自动重启，减少「一会能开一会不能」的情况。

---

## 地址为什么能固定？

启动后会生成本机文件 **`gy-classroom.json`**，写入例如：

```json
{
  "fixed_origin": "http://192.168.1.8:3000",
  "locked": true
}
```

- 二维码、签到链接优先读这个固定地址  
- **不读**临时公网隧道  
- 只要网卡 IP 不变，地址就不变  
- 也可在签到二维码下方点「锁定并应用」手动指定并保存

若 DHCP 换了 IP，程序会检测到并更新固定地址；要彻底避免，请设静态 IP。

---

## 自检清单（扫不开时）

| 检查 | 说明 |
|------|------|
| `start.bat` 窗口还在吗？ | 关掉就等于下课，手机必然打不开 |
| 手机能否打开固定地址？ | 用手机浏览器先打开 `http://192.168.x.x:3000` |
| 是否同一 Wi‑Fi？ | 手机热点 / 访客 Wi‑Fi 往往隔离，扫不开 |
| 是否扫了 localhost？ | localhost 只在老师电脑有效 |
| 防火墙放过了吗？ | 再运行一次 `开放防火墙.bat` |
| 是否还在用 trycloudflare？ | 正式课不要用；改用本机固定地址 |

---

## 不要用的方式

- Cloud Agent / 网页里的临时预览链接  
- `npm run tunnel` / Cloudflare Quick Tunnel  
- 把 `localhost` 打进二维码  

这些都会表现为「一会能访问一会不能」。
