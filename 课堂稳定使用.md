# 本机离线课堂（地址永久固定）

## 你是 Mac：文件在哪？

在**项目文件夹根目录**（和 `index.html` 同一层）找这些：

| 文件 | 作用 |
|------|------|
| **`start.sh`** | Mac 开始上课（不要用 `start.bat`） |
| **`一键放到桌面.command`** | 双击 → 把快捷方式放到桌面 |
| **`一键放到桌面.sh`** | 同上（终端运行也可以） |
| `打开老师端.command` | 打开固定老师端地址 |

### 一键放到桌面（推荐）

1. 用 Finder 打开本机 Photon 项目文件夹  
2. 双击 **`一键放到桌面.command`**  
   - 若提示无法打开：右键 → 打开 → 仍要打开  
   - 或打开「终端」，进入项目目录后执行：  
     `chmod +x 一键放到桌面.sh start.sh && ./一键放到桌面.sh`
3. 桌面应出现：`光影盟-开始上课.command` 或 `GY-Start.command`

### 自己复制也可以

1. Finder 打开项目文件夹  
2. 把 **`start.sh`** 拖到桌面（可复制）  
3. 上课前：打开「终端」，执行：  
   `cd 桌面上的项目路径或复制后仍指向原目录`  
   更稳妥：不要只复制 `start.sh` 单独文件，而是对项目文件夹做**替身（别名）**放到桌面：  
   - 选中项目文件夹 → 右键 → **制作替身** → 把替身拖到桌面  
   - 进入文件夹后双击 `start.sh` / `一键放到桌面.command`

---

## 请收藏这一条（永远不变）

```
http://127.0.0.1:3000/?mode=teacher
```

---

## 每节课（Mac）

1. 电脑连教室 Wi‑Fi（可无外网）  
2. 双击桌面「光影盟-开始上课」或项目里的 `start.sh`（终端窗口保持打开）  
3. 浏览器打开上面的收藏地址  
4. 手机同一 Wi‑Fi 扫码（地址见 `固定访问地址.txt`）

首次需安装 [Node.js LTS](https://nodejs.org/)。  
不要用临时公网隧道。
