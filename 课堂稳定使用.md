# 本机离线课堂（地址永久固定）

## 本机唯一目录

```
/Users/liwei/Photon
/Users/liwei/Photon/index.html
```

所有启动 / 更新 / 桌面快捷方式都指向这里。

| 文件 | 作用 |
|------|------|
| **`/Users/liwei/Photon/start.sh`** | Mac 开始上课（不要用 `start.bat`） |
| **`/Users/liwei/Photon/更新本地.sh`** | 拉取最新代码并校验玩法模式 |
| **`/Users/liwei/Photon/一键放到桌面.command`** | 把快捷方式放到桌面（写死上述路径） |
| `打开老师端.command` | 打开固定老师端地址 |

### 一键放到桌面（推荐）

1. Finder 打开 `/Users/liwei/Photon`  
2. 双击 **`一键放到桌面.command`**  
   - 若提示无法打开：右键 → 打开 → 仍要打开  
   - 或终端执行：  
     `cd /Users/liwei/Photon && chmod +x 一键放到桌面.sh start.sh && ./一键放到桌面.sh`
3. 桌面应出现：`光影盟-开始上课.command`、`光影盟-更新.command`

---

## 请收藏这一条（永远不变）

```
http://127.0.0.1:3000/?mode=teacher
```

---

## 每节课（Mac）

1. 电脑连教室 Wi‑Fi（可无外网）  
2. 双击桌面「光影盟-开始上课」（指向 `/Users/liwei/Photon/start.sh`）  
3. 浏览器打开上面的收藏地址，Cmd+Shift+R  
4. 手机同一 Wi‑Fi 扫码（地址见 `/Users/liwei/Photon/固定访问地址.txt`）

更新代码：双击桌面「光影盟-更新」。

首次需安装 [Node.js LTS](https://nodejs.org/)。  
不要用临时公网隧道。
