# 本机离线课堂（唯一正式方案）

本系统**暂不对外网用户开放**。学生都在教室现场，所有功能在老师电脑本地运行。  
**无需外网**：教室 Wi‑Fi 即使不能上网，只要电脑和手机在同一局域网即可签到、投票、抽人、加分。

不要使用 Cloudflare / ngrok / Cloud Agent 临时链接。

---

## 一次准备（有网时做完即可）

1. 安装 [Node.js LTS](https://nodejs.org/)（只需一次）  
2. 把本仓库拷到教室电脑（U 盘也可，不必每次 git）  
3. Windows：双击一次 **`开放防火墙.bat`**  
4. （强烈建议）给电脑设**静态 IP**，或路由器 DHCP 保留 —— 扫码地址长期不变  

之后可以**完全断网**上课。

---

## 每节课

1. 电脑连教室 Wi‑Fi（与学生同一网络；可无外网）  
2. 双击 **`start.bat`**（窗口一直开着）  
3. 浏览器打开老师端：`http://localhost:3000/?mode=teacher`  
4. 看窗口 **「★ 固定扫码」**（如 `http://192.168.1.8:3000`）  
5. 签到页 →「开启签到」→ 学生微信扫码  
6. 下课关窗口  

异常退出会自动重启。数据在本机 `gy-demo-db.json`，字体/二维码在 `vendor/`，不请求外网。

---

## 网络关系（示意）

```
学生手机 ──同一 Wi-Fi/局域网──► 老师电脑:3000（start.bat）
                                      │
                                      ├─ index.html / vendor（本地静态）
                                      └─ /__gy/demo-db（本机同步签到等）
```

不经过互联网。外网断开不影响使用。

---

## 固定地址

启动后生成 **`gy-classroom.json`**，锁定局域网扫码地址。  
也可在二维码下方点「锁定并应用」。  
IP 若因 DHCP 经常变，请设静态 IP。

---

## 扫不开时检查

| 检查 | 说明 |
|------|------|
| start.bat 窗口开着？ | 关掉 = 服务停了 |
| 手机浏览器能否打开固定地址？ | 先验证局域网通不通 |
| 同一 Wi‑Fi？ | 勿用访客隔离网 / 手机流量 |
| 是否扫了 localhost？ | 只在老师电脑有效 |
| 防火墙？ | 再跑一次 `开放防火墙.bat` |

---

## 明确不用

- 临时公网隧道（trycloudflare / ngrok）  
- Cloud Agent 预览链接  
- Firebase / Vercel 等外网服务（当前已关闭）  
