# 本机离线课堂（地址永久固定）

## start.bat 在哪？如何放到桌面？

`start.bat` 在**项目文件夹根目录**（和 `index.html`、`server.mjs` 同一层）。

第一次拿到程序后，在该文件夹里双击一次：

```
一键放到桌面.bat
```

桌面会出现：

| 桌面图标 | 作用 |
|----------|------|
| **光影盟-开始上课** | 上课前点这个（等于运行 start.bat） |
| 光影盟-打开老师端 | 打开固定地址 http://127.0.0.1:3000/?mode=teacher |
| 光影盟-收藏地址.txt | 写着要收藏的地址 |
| 光影盟-绑定扫码地址 | 仅当手机扫不开且 IP 变了时用 |
| 光影盟-开放防火墙 | 教室电脑首次用一次 |

之后上课只需：桌面双击「光影盟-开始上课」。

---
## 请收藏这一条（永远不变）

```
http://127.0.0.1:3000/?mode=teacher
```

双击 `start.bat` 后用它；也可双击 `打开老师端.bat` / `打开老师端.url`。

---

## 为什么以前会变？

临时公网隧道每天换域名；局域网 IP 若被程序自动改写也会变。  
现在：**老师端写死 127.0.0.1:3000**；**手机扫码地址锁定后绝不自动更换**。

---

## 每节课

1. 电脑连教室 Wi‑Fi（可无外网）
2. 双击 `start.bat`（窗口一直开着）
3. 打开收藏的老师端地址
4. 手机扫码用已锁定地址（见 `固定访问地址.txt`）

首次连教室网启动时会自动绑定扫码地址并写入 `gy-classroom.json`。  
以后只有你主动运行 `绑定课堂扫码地址.bat`（或页面上「重新绑定」）才会换扫码地址。

建议给电脑设**静态 IP**，与锁定地址一致，手机长期可扫。

---

## 一次准备

1. 安装 Node.js LTS（有网时一次）
2. 双击一次 `开放防火墙.bat`
3. 连教室 Wi‑Fi → `start.bat` → 自动绑定扫码地址
4. 浏览器收藏 `http://127.0.0.1:3000/?mode=teacher`

---

## 不要用

临时公网 / Cloud Agent / 每天不一样的隧道链接。
